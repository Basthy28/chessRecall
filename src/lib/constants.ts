/**
 * Shared constants used by both the Next.js server and the standalone worker process.
 * Import from here — never import from the worker file inside Next.js code.
 */

export const ANALYZE_QUEUE_NAME = "analyze-game";
